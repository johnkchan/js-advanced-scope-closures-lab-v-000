function produceDrivingRange(blockRange) {
  return function(a, b) {
    let iA = parseInt(a.slice(0, 2), 10);
    let iB = parseInt(b.slice(0, 2), 10);
    if (b - a <= blockRange) {
      
    } else {
      
    }
  };
}